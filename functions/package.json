{
  "name": "audit-app-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && node scripts/copy-pdf-assets.js",
    "watch": "tsc --watch",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@sparticuz/chromium": "^131.0.1",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "puppeteer-core": "^24.9.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.16.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
